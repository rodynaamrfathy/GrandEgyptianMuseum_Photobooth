export const MAX_TEXT_LENGTH = 60;
export const MAX_TEXT_LINES = 3;
export const CHARS_PER_LINE = 25;

export const IMAGE_ID_PATTERN = /^[a-zA-Z0-9]+_[a-zA-Z0-9]+_\d+$/;

export const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export const FLIP_INTERVAL_MS = 3000;
export const LOADING_TEXT_INTERVAL_MS = 3000;
export const BLOB_REVOKE_DELAY_MS = 2000;

export const RATE_LIMIT_COOLDOWN_MS = 5000;